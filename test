#include <iostream>
#include <string>
#include <string.h>

using namespace std;



int main()
{
    string main, sub;
 int   ps= 0;
    getline(cin, main);
    getline(cin, sub);
    stringFinder(main, sub, &ps);
    if (ps == 1) cout<<"Found";
    else cout<<"Not Found";
}
